import { styled } from "theme";

export const ContentWrapper = styled.div`
    display: flex;
    flex-flow: column wrap;
    flex-basis: 80%;
    align-items: center;

    img {
        width: 100%;
    }
`;