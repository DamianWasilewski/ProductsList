import { styled } from "theme";

export const StyledHeader = styled.div`
    position: fixed;
    top: 0;
    left: 0;
    display: flex;
    flex-flow: row wrap;
    align-items: center;
`;

export const FiltersWrapper = styled.div`
    display: flex;
    align-items: center;
`;